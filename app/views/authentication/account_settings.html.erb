<p>Account Settings</p>
<%= form_for @user, :as => :user, :url => account_settings_path, :method => :put do |f| %>
    <p>
      <%= f.label 'current password:' %>
      <%= f.password_field :password %>
      <%= show_field_error(@user, :password) %>
    </p>
    <p>
      <%= f.label 'username: (leave unchanged if you are not changing your username)' %>
      <%= f.text_field :username %>
      <%= show_field_error(@user, :username) %>
    </p>
    <br/><br/><br/>
    <p>
      <%= f.label 'email: (leave unchanged if you are not changing your email address)' %>
      <%= f.text_field :email %>
      <%= show_field_error(@user, :email) %>
    </p>
    <br/><br/><br/>
    <p>
      <%= f.label 'new password: (leave blank if you are not changing your password)' %>
      <%= f.password_field :new_password %>
      <%= show_field_error(@user, :new_password) %>
    </p>
    <br/><br/><br/>
    <p>
      <%= f.label 'password confirmation:' %>
      <%= f.password_field :new_password_confirmation %>
      <%= show_field_error(@user, :new_password_confirmation) %>
    </p>
    <p>
      <%= f.submit 'Update' %>
    </p>
<% end %>
